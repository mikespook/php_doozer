// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("doozer", "for doozer support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("doozer", "enable doozer support", "no");

if (PHP_DOOZER != "no") {
	EXTENSION("doozer", "doozer.c");
}

